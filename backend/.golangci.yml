run:
  timeout: 5m

linters:
  disable-all: true
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - gosec
    - bodyclose
    - sqlclosecheck

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - gosec
