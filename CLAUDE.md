# OpenRisk — Contexte permanent Claude Code
 
## Projet
OpenRisk est une plateforme GRC enterprise open-source (https://github.com/opendefender/OpenRisk).
Partie de l'écosystème OpenDefender. Marché cible : France, Belgique, Maghreb, Afrique subsaharienne.
 
## Stack technique
- Backend  : Go 1.25 · Fiber v2 · GORM · PostgreSQL 16 · Redis · golang-migrate
- Frontend : React 19 · TypeScript strict · Zustand · Tailwind CSS 3 · Recharts · React Router v7
- Infra    : Docker multi-stage · Kubernetes + Helm · GitHub Actions CI/CD
- Obs.     : Prometheus · Grafana · Loki · zerolog JSON · Sentry
## Architecture obligatoire
Backend — Clean Architecture stricte :
  /cmd/server/main.go         → DI container, graceful shutdown
  /internal/domain/           → entités pures (pas de Fiber, pas de GORM ici)
  /internal/application/      → use cases (1 use case = 1 fichier)
  /internal/infrastructure/   → repositories, messaging, integrations
  /internal/api/http/         → handlers Fiber + middleware
  /pkg/                       → packages partagés (scoring, cti, notify, export, ai, crq)
 
Frontend — Feature-based :
  /src/features/[module]/     → pages, components, hooks, stores par feature
  /src/shared/                → design system, hooks globaux, utils
  /src/services/              → client API typé (généré depuis OpenAPI)
  /src/locales/               → fr.json, en.json
 
## Règles ABSOLUES — jamais violer
1. Lire TOUS les fichiers existants d'un module avant d'écrire une seule ligne
2. Filtrer par tenant_id sur CHAQUE query DB — aucune exception
3. Erreurs typées uniquement : ErrNotFound, ErrForbidden, ErrConflict, ErrValidation
4. Tests minimum par use case : TestXxx_Success + TestXxx_NotFound + TestXxx_Unauthorized
5. Zero `any` TypeScript — tout est typé strictement
6. Jamais de secrets dans les logs (tokens, passwords, clés)
7. Transactions DB sur toute opération multi-table
8. Skeleton loaders côté frontend — jamais de spinner pleine page
9. Toujours gérer les 3 états UI : loading + error + empty
10. Optimistic updates sur toutes les mutations critiques (UX perçue)
11. Zod validation côté client sur tous les formulaires
## Formule Score Engine
Score = Probability (0.0–1.0) × Impact (0.0–10.0) × AssetCriticality (0.1–3.0)
Criticality : score ≥ 7.0 = critical · ≥ 4.0 = high · ≥ 2.0 = medium · < 2.0 = low
 
## Ce qui est déjà implémenté
**`ROADMAP.md` est réécrit et cartographié sur le Master Prompt V5 (2026-07-10)** : inventaire complet **module par module** (0→17 + 14.1–14.18 + 17.1–17.7 + Partie C), avec statut réel (✅/🟡/❌) établi par lecture du code, et le détail des modules prouvés live. **C'est la source de vérité — le consulter avant tout.** Résumé exécutif : fondations GRC (Modules 0–13) largement livrées et prouvées (Score/Risk/Mitigation/Compliance FR+africain/Assets/Dashboard/Notifications/Reporting+Board Report) ; sur 18 fonctionnalités avancées (14.x) **1 faite (14.12 Board Report), 5 partielles, 12 absentes** ; Auth au-delà du login, CTI, IA Advisor complet, SSE hub, Billing, Onboarding, Super-Admin = à faire. **Scanner (Module 6) : COMPLET + prouvé live de bout en bout le 14/07/2026 (voir items 16-17)** — backend + collectors SDK réels AWS/Azure/GCP + binaire Agent (nmap) + notif in-app/email + frontend ; ne restent que les alerts cloud (Defender/SCC). Détail historique ci-dessous :
- **Solide (✅)** : Risk Register, Mitigation, Score Engine, Notifications, Audit logging, i18n FR/EN, Dashboard/Analytics (login → dashboard vérifié live le 08/07/2026, plus de déconnexion forcée), **Compliance Frameworks — M1 complet, vérifié live de bout en bout le 08/07/2026** (use cases + handlers + OpenAPI + client généré + frontend + upload de preuve réel + RBAC granulaire, voir `ROADMAP.md` §3 M1), **M2 ISO 27001:2022 — 93 contrôles Annexe A importables en un clic depuis un catalogue générique et extensible, vérifié live le 08/07/2026** (voir `ROADMAP.md` §3 M2), **M3 Assets — inventaire complet, fait le 08/07/2026** (Clean Architecture rétrofitée depuis un handler qui touchait `database.DB` directement sans aucune RBAC ; snapshots historiques ; criticité enfin branchée sur le Score Engine via le flux Redis `asset.criticality_changed` qui existait déjà côté `ScoreWorker` mais n'avait jamais d'émetteur ; voir `ROADMAP.md` §3 M3). Auth (JWT RS256, Argon2id, MFA, OAuth2/SAML2) et RBAC multi-tenant : code présent et maintenant fonctionnel de bout en bout (login réel testé le 08/07/2026 après correction de 11 bugs, voir `ROADMAP.md` §3 M1) mais **jamais vérifié en profondeur au-delà du login/dashboard** — traiter chaque sous-flux (MFA, OAuth2/SAML2, refresh token) comme non prouvé tant qu'il n'a pas été testé live.
- **Partiel (🟡)** : Incident Management (table manquante d'`AutoMigrate`), Custom Fields, Marketplace (structs sans tags `gorm:`, exclues d'`AutoMigrate` le 08/07/2026), PAM Audit Trail, CTI Engine (non câblé, en avance de phase Wave 2). Deux widgets Dashboard (`SecurityScore`, `AssetStatistics`) appellent des endpoints backend qui n'existent pas (`/analytics/security-score`, `/analytics/assets/statistics`) — repli gracieux côté frontend, sans conséquence UX, mais pas de vraies données tant que ces routes ne sont pas implémentées. `CreateRisk` ne renseigne pas `created_by` depuis le contexte réel (reste `uuid.Nil`).
- **Non commencé (❌)** : Offline-first, Billing/Stripe, différenciateurs Wave 2/3. (Le contenu réglementaire africain, longtemps reporté faute de textes source fiables, est désormais **implémenté** — catalogues de contrôles cités BCEAO/UEMOA, ANTIC-CM et COBAC ajoutés le 08/07/2026 sur la base des textes fournis par l'utilisateur ; voir item 12 du sprint. Seul reste en placeholder le cadre camerounais de protection des données personnelles, faute de texte source.)

## ⚠️ Découverte critique du 08/07/2026 — à connaître avant de toucher Risk/Asset/Mitigation/Dashboard/Multitenancy
`middleware.SetContext()` n'était appelé **nulle part en code de production** (seulement dans un harnais de test) — `middleware.GetContext(c)`, lu par 8 handlers (`compliance_handler.go`, `risk_handler.go`, `asset_handler.go`, `mitigation_handler.go`, `mitigation_subaction_handler.go`, `dashboard_handler.go`, `multitenancy_auth_handler.go`, `multitenancy_org_handler.go`), retournait donc toujours `nil`, et ces handlers retombaient silencieusement sur `tenant_id = uuid.Nil`. Corrigé en une ligne dans `AuthMiddlewareRS256` (`internal/middleware/auth.go`). **Toute affirmation antérieure au 08/07/2026 sur le bon fonctionnement multi-tenant de ces modules doit être revérifiée** — voir `ROADMAP.md` §3 M2 pour le détail complet.

## Priorités du sprint en cours
1. ~~Corriger le build frontend~~ ✅ fait le 07/07/2026 (branche `fix/frontend-build-typescript-errors`, commit `4e5b91f5`).
2. ~~Corriger le bug cross-tenant `GormComplianceRepository.UpdateControl`~~ ✅ fait le 07/07/2026 (branche `fix/compliance-cross-tenant-update-isolation`, commit `5a0407fc`).
3. ~~M1 Compliance — use cases + handlers + OpenAPI + frontend~~ ✅ fait le 07/07/2026 (branche `feat/m1-compliance-engine`, voir `ROADMAP.md` §3 M1 pour le détail des 6 commits et des preuves).
4. ~~Vérification manuelle live de M1 + bug d'amorçage `AutoMigrate`~~ ✅ fait le 08/07/2026 (même branche) — 8 bugs corrigés en chaîne, l'app n'avait jamais eu de login fonctionnel dans cet environnement ; détail complet dans `ROADMAP.md` §3 M1.
5. ~~Dashboard : déconnexion forcée après login~~ ✅ fait le 08/07/2026 (même branche, signalé en direct par l'utilisateur) — 3 bugs de plus (clé de contexte `userID`/`user_id` incohérente, `/risks` sur le même bug `RequirePermissions` legacy que Compliance, intercepteur axios trop agressif sur tout 401) ; détail complet dans `ROADMAP.md` §3 M1.
6. ~~M2 — ISO 27001:2022~~ ✅ fait + vérifié live le 08/07/2026 (même branche) — catalogue générique + 93 contrôles + import en un clic + bug fondamental de multi-tenancy découvert et corrigé (`SetContext` jamais appelé en prod, voir avertissement ci-dessus) ; détail complet dans `ROADMAP.md` §3 M2.
7. ~~M3 — Assets~~ ✅ fait le 08/07/2026 (branche `feat/m3-assets-inventory`) — Clean Architecture rétrofitée (le handler existant touchait `database.DB` directement, zéro use case, zéro RBAC sur `POST /assets`), snapshots historiques, criticité enfin branchée sur le Score Engine (bug de scan varchar→float64 dans `GetRisksByAssetID` corrigé au passage, jamais atteint en pratique faute d'émetteur Redis) ; détail complet dans `ROADMAP.md` §3 M3.
8. ~~Bugs live découverts en vérifiant "Compliance a disparu de la sidebar"~~ ✅ fait le 08/07/2026 (branche `fix/dashboard-crash-mitigation-routes-and-ui-polish`) — Compliance n'avait pas disparu, c'est le **dashboard entier qui crashait en page blanche** après login : `stats_handler.go` scannait des colonnes NUMERIC Postgres dans des champs Go `int` (500 systématique sur risk-matrix/trends/top-vulnerabilities, jamais atteint avant faute de vraies données) ; corriger ça a exposé un champ `severity` manquant et une échelle de probabilité fausse (1-5 au lieu de 0.0–1.0) qui crashaient `TopVulnerabilities`/`RiskMatrix` ; `StatusDot.tsx` n'avait pas de `default` et crashait sur tout statut hors de son union stricte (domain.RiskStatus a DEUX vocabulaires incompatibles en usage — `open/in_progress/...` et `DRAFT/ACTIVE/...`). En creusant sur Mitigations (401 partout), trouvé que **`middleware.RequireRole` lit `c.Locals("role")`, une clé jamais posée par `AuthMiddlewareRS256`** (qui pose `org_roles`, une map) — toutes les routes d'écriture Mitigations/Incidents/Risk-Management renvoyaient 401 pour tout le monde, tout le temps ; corrigé + ajouté la route `GET /mitigations` qui n'existait tout simplement pas. Polish demandé au passage : overflow sidebar (20 items sans scroll), transition de route globale (`AnimatePresence` dans `App.tsx`), skeleton+stagger sur les widgets dashboard. Détail complet dans le message de commit.
9. ~~Master Prompt V4 (vision long terme fournie par l'utilisateur)~~ ✅ intégré le 08/07/2026 — sauvegardé dans `docs/MASTER_PROMPT_V4.md` (condensé + croisé avec l'état réel de chaque module), référencé en tête de `ROADMAP.md`.
10. ~~M4 — Reporting officiel, partie « rapport de conformité en 1 clic »~~ ✅ fait + vérifié live le 09/07/2026 (branche `feat/m4-compliance-report-pdf`). Endpoint `GET /compliance/frameworks/{id}/report?locale=fr|en` → PDF soigné tenant-scoped (page de garde + synthèse exécutive graduée + tableau paginé des contrôles avec statut coloré/preuves/citation source). Clean Architecture : `backend/pkg/report` (renderer **pur**, testé ; piège `fpdf.SplitText` qui panique sur rune > 255 — tiret cadratin, ligature œ, apostrophe typographique — contourné par un word-wrap maison `wrapText` basé sur `GetStringWidth`) + `GenerateComplianceReportUseCase` + `CountEvidencesByFramework` (requête groupée, isolation tenant preuve ET contrôle) + handler + bouton front « Rapport PDF » (i18n FR/EN, `complianceService.downloadReport`) + OpenAPI. Preuve live : rapport ISO 27001 (framework à 198 contrôles ISO+africains, 11 pages, 27,6 %), en-tête de tableau répété par saut de page, accents FR (ANTIC-CM) et texte EN (ISO) corrects côte à côte. Détail complet dans `ROADMAP.md` §3 M4. **Board Report mensuel IA/FCFA : désormais ✅ fait le 10/07/2026 (voir item 15) — M4 complet.** Branches ouvertes en attente de PR/merge : `feat/m3-assets-inventory`, `fix/dashboard-crash-mitigation-routes-and-ui-polish`, `feat/africa-compliance-catalogs-and-responsive` (base de la branche M4 courante) — aucune mergée dans master.
12. ~~Compliance africaine + fixes UI + responsive + nettoyage repo~~ ✅ fait le 08/07/2026 (même branche `fix/dashboard-crash-mitigation-routes-and-ui-polish`, 4 commits). **(a) Compliance :** les 3 placeholders `cobac`/`bceao`/`anssi-cm` (jusque-là `Available:false`, faute de textes source) sont devenus de vrais catalogues cités article par article, l'utilisateur ayant fourni les textes : `bceao` (35 contrôles — Règlement n°15/2002/CM/UEMOA + instructions surveillance/monnaie électronique/CIP + Avis e-relevés), `antic-cm` (25 contrôles — Loi n°2010/012 du Cameroun ; l'ancien `anssi-cm` mal attribué est remplacé par `antic-cm`, le vrai régulateur), `cobac` (45 contrôles — Règlement COBAC R-2016/04 contrôle interne CEMAC). Mécanisme inchangé (import per-tenant, même moteur qu'ISO 27001) ; `TestNoOrphanControls` garantit code de référence unique + citation source par contrôle ; un seul placeholder subsiste (`cm-loi-2024-017`, protection des données perso, texte non fourni) pour garder vivant le chemin « coming soon » + le garde-fou d'import indisponible. **(b) Risks toasts en rafales :** le client ouvrait un flux SSE sur `/api/v1/risks/events` (route **inexistante côté backend**) et `RiskListPage` levait un toast « server error » à chaque erreur SSE pendant qu'`EventSource` reconnectait ; aggravé par `useSSE` qui listait les callbacks inline dans les deps de `connect` → `connect` recréé à chaque render → effet de montage relancé (disconnect+reconnect) à chaque render. Corrigé : callbacks dans des refs (connect/disconnect stables, cap de reconnexion effectif) + l'erreur SSE est un simple log en dev (les updates live sont un bonus, la liste refetch sur chaque mutation). **(c) Mitigations invisible :** la route `/mitigations` + la page existaient mais aucun item de sidebar → ajouté (icône Wrench). **(d) Responsive :** Sidebar en tiroir off-canvas < lg (backdrop + bouton), colonne statique repliable ≥ lg ; barre supérieure mobile avec hamburger dans `DashboardLayout` ; header Dashboard adaptatif ; tables larges (Risks, Assets, ControlTable) en `overflow-x-auto` + min-width. `tsc -b` et `vite build` OK. **(e) Nettoyage repo** (validé par l'utilisateur) : suppression de `init_structure.sh` (vide), `FILES_CREATED.sh`, `SECURITY_AUDIT_HANDLERS.csv`, `add_headers.py`.
14. ~~Pass UX post-M4 + suppression de framework + isolation multi-tenant des frameworks~~ ✅ fait les 09-10/07/2026 (branche `feat/m4-compliance-report-pdf`, 4 commits, tout vérifié live via Chrome CDP + poussé). **(a) 5 bugs UI signalés :** page blanche au clic d'un risque (`RiskBadge` crashait sur `level` minuscule "medium" — switch sans `default` → normalisation + fallback LOW) ; Mitigations en chargement infini (`loadMitigations` dépendait du store entier ET appelait `setMitigations` dedans → boucle ; corrigé via sélecteurs stables) ; Compliance/Risks non scrollables (`h-full overflow-y-auto` au lieu de `min-h-screen`/rien, dans un `main` en `overflow-hidden`) ; import de catalogue pas intuitif (chaque catalogue crée/réutilise désormais **son propre** framework via `useImportCatalogAsFramework`, bouton d'import en tête toujours accessible). **(b) Modals cachant leur bouton submit** (Risk, Create/Edit Asset, Create Mitigation) : centrés verticalement sans `max-h`/scroll → restructurés en `max-h-[90vh]` flex col avec header fixe + corps scrollable + footer épinglé (vérifié visible à 600-650px). **(c) Suppression de framework** : `DELETE /compliance/frameworks/{id}` admin-only (cascade les contrôles du tenant), `DeleteFrameworkUseCase` + repo + bouton corbeille dans le rail + confirmation. **(d) Nettoyage ISO** : le framework ISO 27001 avait 198 contrôles (ISO+africains mélangés par l'ancien flux d'import) → ramené à ses 93 vrais contrôles ISO (105 non-ISO supprimés via l'API). **(e) Terminologie catalog/framework** unifiée dans l'UI (« Ajouter un référentiel » primaire / « Référentiel vierge » secondaire ; « catalogue » retiré du vocabulaire utilisateur). **(f) 🔴 Frameworks rendus tenant-scoped** (étaient globaux — `tenant_id` ajouté partout : domaine/repo/use cases/handlers, index unique par tenant `(tenant_id,name,version)`, migration `0030` + backfill DB dev, tests d'isolation cross-tenant) ; **prouvé live** : un nouveau tenant voit 0 framework pendant que l'admin voit ses 4, et peut créer son propre « ISO 27001/2022 » sans conflit. Consigne design enregistrée en mémoire (`feedback-frontend-apple-grade-ux`).
15. ~~M4 seconde moitié — Board Report mensuel (IA, ton non-technique, montants FCFA, human-in-the-loop)~~ ✅ fait + vérifié live le 10/07/2026 (même branche `feat/m4-compliance-report-pdf`). **Premier client LLM câblé du projet.** Livré selon le plan : nouveau package `pkg/ai` (interface `Advisor` + `TemplateAdvisor` déterministe FR/EN sans clé, testé + `ClaudeAdvisor` via le SDK officiel `anthropics/anthropic-sdk-go`, modèle `claude-opus-4-8`, adaptive thinking, JSON strict, cf. skill `claude-api` ; factory `NewAdvisor` → Claude si `ANTHROPIC_API_KEY` sinon Template) → `internal/application/board` (`GenerateBoardReportUseCase` agrège la posture **réelle tenant-scoped** : risques par criticité via `CountRisksByCriticality` — méthode concrète sur `GormRiskRepository`, PAS sur l'interface `RiskRepository`, pour ne pas casser les mocks ; conformité par référentiel réutilisant `ComplianceRepository` ; exposition FCFA via `ExposureModel` — valeurs de référence par criticité, ordre de grandeur assumé/documenté ; **retombe sur le TemplateAdvisor si l'appel LLM échoue**, `generated_by_model` trace la provenance) + Get/List/Update(brouillon)/Approve(fige)/Delete → `domain.BoardReport` (snapshot posture gelé + narration éditable + draft→approved, dans `AutoMigrate`) + `GormBoardReportRepository` (tenant-scoped) → `pkg/report/board_pdf.go` (`RenderBoardPDF` pur, réutilise les helpers du rapport de conformité, badges statut, cartes KPI, barres par référentiel, footer « BROUILLON » ; testé accents+tiret cadratin) → handler + routes `reports:board:*` → front `features/reports/BoardReportPage.tsx` (React Query `useBoardReports`/`useBoardReport`, `boardService`, item sidebar + route, 3 états UI, narration éditable, `tsc -b` OK) + OpenAPI. Preuve live : flux complet exercé (génération→édition→approbation→400 si ré-édition→PDF inspecté en PNG), agrégation réelle des 4 référentiels du tenant, 3 000 000 FCFA, `created_by` renseigné. **Le chemin ClaudeAdvisor reste non prouvé live faute d'`ANTHROPIC_API_KEY`** (il compile + fallback template fonctionnel) — à revérifier dès qu'une clé est fournie. Détail complet dans `ROADMAP.md` §3 M4. **M4 est désormais complet.**
16. ~~Compliance redesign dc.html — gestion complète des référentiels/contrôles + preuve intuitive + statut→progression temps réel + seuil de preuve obligatoire~~ ✅ fait + vérifié live le 13/07/2026 (branche `feat/ui-redesign-dc-html`). Les écrans redessinés (`ComplianceScreen` + `FrameworkDetail`) n'avaient hérité d'**aucune** action de gestion (la logique existait dans l'ancien `CompliancePage` orphelin). Livré : **(a)** nouveaux dialogs dc.html (`ComplianceModals.tsx` : créer un référentiel vierge, importer un référentiel du catalogue, ajouter un contrôle ad-hoc) branchés sur les hooks existants ; **(b)** suppression référentiel (par carte) + suppression contrôle (par ligne), gardées par permission (`compliance:frameworks|controls:delete`, admin uniquement via `hasPermission`) ; **(c)** bouton « Preuves » refait en **chip cliquable** montrant le nombre (`1 preuve` / `Ajouter` en pointillé si 0) — les utilisateurs ne comprenaient pas qu'il était cliquable ; **(d)** **progression temps réel** : la jauge d'en-tête de `FrameworkDetail` est calculée localement depuis les contrôles chargés (met à jour l'update optimiste instantanément), et toutes les mutations invalident désormais la query combinée `['compliance','overview']` (source des cartes de la grille + en-tête) — clé partagée exportée depuis `useCompliance.ts` ; **(e)** **seuil de preuve obligatoire (mode strict)** : passer un contrôle à « Implémenté » sans ≥1 preuve est bloqué **côté backend** (`UpdateControlUseCase` renvoie `ErrValidation` "evidence_required", vérifie les `Evidences` déjà préchargées par `GetControlByID`, seulement sur la transition *entrante*) **ET côté frontend** (pré-check + toast + ouverture du tiroir de preuves pour guider). Nouveau champ calculé `EvidenceCount` (`gorm:"-"`) rempli par `ListControlsUseCase` via `CountEvidencesByFramework` (une requête groupée, pas de N+1), exposé en OpenAPI (`evidence_count`) → types régénérés (au passage : la génération a resynchronisé les endpoints board-report en attente, additif). Tests : 4 nouveaux cas use-case (bloqué sans preuve / autorisé si autre statut / non re-bloqué si déjà implémenté / `ListControls` attache le compte) + E2E handler réordonné (upload preuve AVANT « implémenté », + assertion 400 sans preuve). Preuves live (API + CDP headless) : create→400 strict→upload→count=1→200→progression 100 %→delete contrôle/référentiel 204 ; captures des 2 écrans. `go build`/`go test ./internal/application/compliance/...` verts, `tsc -b`/`vite build` verts.
17. ~~M5 (amorce) — Incident Management rendu réel + registre d'incidents live~~ ✅ fait + vérifié live le 13/07/2026 (branche `feat/ui-redesign-dc-html`). Le module Incident existait (domain + service legacy + handler + routes déjà câblées dans `main.go`) mais **toute route `/incidents` renvoyait 500/400** à cause de **3 bugs** corrigés : **(a)** les tables `Incident`/`IncidentTimeline`/`IncidentAction` n'étaient **pas dans `AutoMigrate`** → table inexistante → 500 ; ajoutées. **(b)** `GetIncident` **et** `ListIncidents` faisaient `.Preload("Risk")` alors que `domain.Incident` n'a **aucune** association `Risk` (juste un `RiskID *uint`, et les risques utilisent des PK uuid — jointure impossible) → 500 ; preloads retirés. **(c)** les handlers par ID lisaient `c.Params("incidentId")` alors que les routes déclarent `:id` → `ParseUint("")` → 400 ; corrigé (`incidentId`→`id`, + `GetIncidentsForRisk` lisait `riskId` au lieu de `id`). Isolation tenant intacte (le service filtre `tenant_id`, et `AuthMiddlewareRS256` pose bien `c.Locals("tenant_id")` en uuid que `safeGetString` stringifie). Nouveau **registre d'incidents live** côté front (dc.html) : `features/incidents/incidentService.ts` + `useIncidents.ts` (React Query list/stats/create/update-statut/delete) + `IncidentsScreen.tsx` (bandeau KPI depuis `/incidents/stats`, chips de filtre par statut, table titre/type/sévérité/statut éditable/signalé, dialog de création, changement de statut inline, suppression, 3 états UI, gardé par rôle admin/analyst = `writerRole` backend). `/incidents` route désormais vers ce registre ; la console **War Room** (fixtures — pas de backend chat/roster) reste accessible en **Aperçu** à `/incidents/war-room` (bouton dans l'en-tête). Nav renommée « War Room »→« Incidents », faux badge « 1 » retiré. Tests : `incident_handler_test.go` (sqlite in-memory) — E2E create→stats→list→get-by-id→update statut→timeline→delete + isolation cross-tenant + statut invalide. Preuves live (API + CDP headless) : les routes `/incidents/*` passent de 500/400 à 200/201/204, flux complet exercé (stats critical=1/resolved=1/rate=100 %, timeline à 2 événements) ; captures du registre + du dialog de création. `go vet`/`go test ./internal/handler/...` (incidents) verts, `tsc -b`/`vite build` verts. **Restes explicitement hors périmètre** (dette du module legacy) : `RiskID *uint` ↔ risques uuid = `LinkRisk`/`GetIncidentsForRisk` sémantiquement cassés (non exposés dans le registre) ; console War Room non branchée (chat/roster sans backend) ; pas de retrofit Clean Architecture du service (reste un `service` sur `database.DB`, comme avant).
18. ~~Incident UX (détail/édition + War Room réelle + export) + déconnexion + passe d'animation~~ ✅ fait + vérifié live le 13/07/2026 (branche `feat/ui-redesign-dc-html`). **(a) Déconnexion :** le store avait `logout()` mais **aucun bouton** — la barre latérale (`Sidebar.tsx`) affiche désormais un **menu de compte** (clic sur l'avatar) → en-tête nom/email + « Paramètres » (→ /settings) + « Se déconnecter » (rouge, `logout()` + `navigate('/login')`). **(b) Clic sur un incident → drawer détail/édition** (`IncidentDrawer.tsx`) : en-tête réf/sévérité/statut, champs éditables (titre, description, sévérité, statut, assigné, résolution — bouton Enregistrer désactivé si non modifié), **chronologie réelle** (`useIncidentTimeline`), boutons War Room + Supprimer ; gardé par rôle (lecture seule si non-writer). **(c) War Room câblée sur un incident réel** : route `/incidents/:id/war-room`, en-tête réel (titre/sévérité/statut), **durée live** calculée depuis `created_at` (figée si `resolved_at`), fil = **chronologie réelle** du backend, action **« Clore l'incident »** persistée (statut→closed) ; roster/tasks/chat restent des fixtures honnêtement badgées « Aperçu » + input « éphémère ». **(d) Export CSV des incidents** : `exportIncidentsCsv` (client-side, respecte le filtre) → bouton « Exporter » sur le registre + carte « Registre d'incidents » sur l'écran Rapports. **(e) Meta partagée** `incidentMeta.ts` (SEV/STATUS/labels) réutilisée par registre/drawer/war-room ; `incidentService.get(id)` + hooks `useIncident`/`useIncidentTimeline` ajoutés. **(f) Passe d'animation** : stagger `or-fadeup` sur les cartes KPI + lignes du registre (délai plafonné), cartes Rapports & grille Conformité ; le drawer/dialogs utilisent `or-slidein`/`or-scalein`. Le shell avait déjà les transitions de page globales (`AnimatedOutlet`) + le garde `prefers-reduced-motion` (index.css:222) — vérifié, non redondé. Aucune modif backend cette itération. Preuves live (CDP headless) : menu de déconnexion (Paramètres + Se déconnecter), drawer d'incident (INC-2 « Phishing ciblé », champs + Enregistrer + War Room + Supprimer), War Room réelle (`/incidents/2/war-room`, statut OUVERT pulsant, durée 00:02:00 live, « Incident created » réel, « Clore l'incident »). `tsc -b`/`vite build` verts. ROADMAP.md mis à jour (14.1 → ✅ base ; ligne Compliance ; priorité #4 barrée). **Note honnête :** « polir chaque page à fond » est un chantier ouvert — la feature Incidents est entièrement polie + niceties globales, mais une passe d'animation exhaustive sur les ~16 écrans reste à faire (cf. item 13).
19. ~~Risk module — correctifs bloquants + Login + vues Risk/Mitigation + endpoints doc~~ ✅ fait + vérifié live le 13/07/2026 (branche `feat/ui-redesign-dc-html`). Lot de correctifs signalés par l'utilisateur, tous prouvés live (CDP headless, viewport 1440). **🔴 Bugs bloquants :** **(a)** « Nouveau risque » **page blanche** : `ScoreEngineVisualizer` faisait `metrics.risk_stats.map`/`matrix.matrix` sans garde → `TypeError null.map` → crash React de toute l'app ; gardé (`metrics.risk_stats?.length`, `matrix.matrix &&`) **ET** `App.tsx` bascule sur le modal redesign `features/risks/CreateRiskModal` (sans visualizer, échelle P×I×AC correcte) au lieu du vieux `components/CreateRiskModal` ; `onCreated` rafraîchit le store. **(b) Impossible de créer une mitigation** : le front postait `POST /mitigations` → **405** ; la vraie route est `POST /risks/:id/mitigations` (risk-scoped, `due_date` RFC3339) → `mitigationService.createMitigation` corrigé (risk_id dans le path, date normalisée) ; prouvé 201 live. **(c) Import risques sans retour** : lien retour « Registre des risques » ajouté (`pages/ImportRisks.tsx`). **(d) Bouton Filtres inerte** → ouvre une barre de recherche (nom/actif/référentiel/responsable). **(e) Menu 3 points inerte** → menu Voir/Éditer/Exporter CSV/Supprimer. **(f) Détail risque non éditable/exportable** → boutons Éditer (ouvre `EditRiskModal`) + Exporter (CSV client-side) câblés ; bouton « Créer une mitigation » ouvre `CreateMitigationModal` **lié au risque** (`riskId` → `risk_id`). **🎨 Login :** thème togglable (bouton soleil/lune, avant connexion) ; **vrai logo OpenRisk** (nouveau `shared/Logo.tsx` — bouclier + cible radar, remplace les `Shield` Lucide dans Sidebar + Auth ; `public/openrisk.svg` + favicon + titre « OpenRisk — GRC ») ; **citations rotatives** réelles/attribuées (Schneier, Spafford, Deming, Buffett, Franklin, Einstein… 12 entrées, fondu toutes les 7 s). **📊 Vues :** Risques = sélecteur **Table | Matrice** (heatmap 5×5 probabilité×impact, risques placés en pastilles cliquables → drawer) ; Mitigations = **Kanban | Table | Gantt** (Gantt réel : barres positionnées sur un axe temps depuis `startISO`/`dueISO`). **📄 Doc :** `docs/ENDPOINTS.md` (référence exhaustive des ~190 routes extraites de `main.go`, groupées par module, perms notées). **⏸️ Reporté (trop gros / backend) :** War Room avec **participants + invitations + notifications (in-app/email/Slack/Teams) + suivi d'actions qui résout l'incident** — nécessite table `incident_participants`, endpoints d'invitation, modèle d'actions persistées, et câblage `pkg/notify` ; plan détaillé consigné dans `docs/ENDPOINTS.md` §« Planned » et ROADMAP 14.1. `tsc -b`/`vite build` verts.
16. ~~Module 6 — Infrastructure Scanner, backend complet (interface Scanner + implémentations cloud + logique Agent + modèle)~~ ✅ fait + **vérifié live de bout en bout le 14/07/2026** (branche `feat/scanner-engine`). Architecture Clean respectée. **Domaine** (`domain/scanner.go`) : `AssetType`, `ScannerProvider` (aws/azure/gcp/nmap/agent, `IsAgentBased()` = nmap+agent), `ScanConfig` (tenant-scoped, creds chiffrées, `AgentIDs`), `ScannerAgent`, `ScanJob` (+ 3 repos, isolation tenant sur chaque query) ; dans `AutoMigrate` (`scan_configs`/`scanner_agents`/`scan_jobs` créées live). **`internal/scanner/`** : `Scanner` interface + DTOs discovery (`AssetDiscovery`/`FindingDiscovery` — CPE, criticité inférée), **pipeline** `Validate→Scan→Normalize→Deduplicate→StorePreview→Notify` qui **ne crée JAMAIS d'Asset/Risk** (tout en preview Redis `PreviewStore` TTL 48h) ; normalize (CPE lower/dedup/tri, criticité inférée env+tags → [0.1,3.0], sévérité) ; dedup (par ExternalID+tenant, merge CPE/tags/max-crit) ; **auto-mitigation par diff** findings N-1→N ; notifier SSE (`RedisNotifier`) + seam `InAppSink` ; scanners cloud AWS/Azure/GCP (`cloudScanner` + seam `CloudCollector` — nil → **preview vide honnête avec raison, jamais de faux résultat** ; map SDK documentée EC2/S3/SecurityHub…) ; scanners agent nmap/agent (`Validate` = scope **≤/24**) ; **agentauth** (token scopé `scanner` 7j + registration token 24j + `HashToken` SHA-256 + `HMAC-SHA256` push sign/verify) ; **lock Redis** (1 scan/config + 3/tenant via `SetNX`). **`application/scanner/`** : `CredentialCipher` (AES-256-GCM, clé `SCANNER_CREDENTIAL_KEY`), create/list/get/delete config, `TriggerScan` (concurrence + lock + cloud async goroutine / dispatch agent SSE), register/list/revoke agent, `PushResults` (claim job + ingest + HMAC vérifié en handler), heartbeat, list jobs, get preview, **import** (seul endroit qui écrit en DB, sur action user, criticité éditable) + ignore. **Handlers** `scanner_handler.go` + `scanner_agent_handler.go` (SSE via `SetBodyStreamWriter`). **Câblage** : ⚠️ **piège Fiber** — `api.Use(Protected)` et le `protected.Use(RequireRole)` de la marketplace sont montés sur le préfixe `/api/v1` et enveloppent donc TOUTE route sous ce préfixe ; les routes agent (`/scanner/agents/register`, `/agent/stream`, `/agent/push`) sont donc enregistrées sur `app` **avant** ces middlewares (closures forward-déclarées) pour s'auto-authentifier sans les hériter. Routes user sous `protected` avec `RequirePermission("scanner:*")`. **~30 tests** (pures : ValidateTarget scope, inférence criticité, dedup, mitigation diff, HMAC ; pipeline Ingest ; use cases Success/NotFound/Unauthorized/Conflict avec mocks). Build+vet+`-race` verts. **Preuve live** (Postgres:5434+Redis, port 8090) : login→create config agent→trigger (job queued+dispatch SSE)→registration-token 24h→register agent (token scopé 783c + push_secret 32o)→**push HMAC** (2 assets dédupliqués→1, CPE mergés, sévérité normalisée, CVE upcasée ; mauvaise signature→401)→preview (criticité inférée prod+internet-facing→3.0)→**import** (override CRITICAL→Asset `source=SCANNER` en DB)→scan #2 sans finding→**auto-mitigation détectée**→**creds AWS chiffrées en DB** (0 plaintext, absentes de la réponse), cloud scan→**preview vide honnête** (« live enumeration not configured »)→**révocation agent** (DELETE 204, push post-révocation→401). Restent (non bloquants, prochaine session) : énumération SDK cloud réelle (collector), binaire Agent (Go <15 Mo nmap/osquery/systemd/tray/auto-update), frontend `features/infrastructure`, notif in-app/email sur scan. Détail dans `ROADMAP.md` Module 6.
17. ~~Module 6 — 4 follow-ups (collectors cloud réels + binaire Agent + frontend + notif)~~ ✅ fait + **vérifié live le 14/07/2026** (branche `feat/scanner-engine`, poussée). **(a) Notif in-app + email sur fin de scan** : le seam `InAppSink` (était nil) est câblé → `TriggeredBy` propagé `PreviewMeta→ScanPreview` ; `notification.UseCase.NotifyInApp` + `Repository.CreateNotification` (impl Gorm + mock) crée une notif `in_app` (Type `scan_complete`) pour l'utilisateur déclencheur + email best-effort via `emailTransport.SendEmail`. **Bug pré-existant corrigé** : `domain.Notification`/`NotificationPreference` **jamais dans `AutoMigrate`** → toute la feature /notifications plantait sur table absente ; ajoutées (Metadata jsonb reste NULL). Preuve : scan → unread 0→1, sujet « Scan complete — review pending ». **(b) Collectors SDK réels** `internal/scanner/collectors` (package séparé pour garder le pipeline sans SDK lourds) : **AWS** (aws-sdk-go-v2 : EC2 DescribeInstances toutes régions, S3 ListBuckets + GetBucketEncryption misconfig, Security Hub GetFindings ≥ MEDIUM), **Azure** (Resource Graph, 1 requête KQL → VMs/App Services/SQL/Storage/Key Vaults via ClientSecretCredential), **GCP** (Compute AggregatedList toutes zones via SA JSON). Câblés dans le registry (creds déchiffrées au scan). Preuve : les 3 SDK **réellement appelés** (fake creds → EC2 401 réel / échec auth AAD réel / erreur parse clé GCP réelle), job completed avec l'erreur réelle + preview vide honnête. Restent Defender (Azure) + SCC (GCP) alerts. **(c) Binaire Agent** (module `agent/`, **6,5 Mo** stripped, stdlib pur — aucune dépendance) : flags/env, register (token 24h → token scopé + push secret persistés 0600), heartbeat 30s (nouveau endpoint `POST /scanner/agent/heartbeat`), **SSE jobs** (reconnexion backoff), **nmap** `-sV --script vuln` (+`-O` si root) + osquery si présent, **parse XML** → assets (ports/CPE/OS) + findings (CVE via regex sur output vuln), **push HMAC-SHA256**, scope ≤/24, timeout 15 min, stateless, **auto-update** (GitHub Releases 24h), `-install` → unit systemd, README (Windows/macOS/Docker). **Livraison de jobs robuste** : **replay des jobs `queued` en préambule SSE à chaque (re)connexion** (Redis pub/sub est fire-and-forget → un job publié pendant une reconnexion serait perdu) ; le handler reçoit `ScanJobRepository`. Preuve live complète : agent enrôlé → SSE → job reçu → **nmap réel sur 127.0.0.1/32 → 1 actif (14 ports) + 22 CVE réels (OpenSSH 9.6p1)** → push HMAC vérifié → job completed → preview importable ; auto-update a trouvé un tag GitHub plus récent ; agent online ; révocation → 401. **(d) Frontend** `features/infrastructure` : `scannerService.ts` (typé, zéro `any`) + `useScanner.ts` (React Query ; les jobs **poll** tant qu'un scan est en vol car EventSource ne peut pas envoyer le header Bearer) + `scannerMeta.ts` ; **InfrastructurePage** (cartes providers AWS/Azure/GCP/On-Premise + compteur agents online, configs scan/deploy/delete, agents online+révoquer, scans récents poll→preview), **ScanConfigDrawer** (formulaires creds par provider / cibles CIDR), **AgentDeployModal** (token 24h + downloads par OS détecté + commande docker), **ScanPreviewPage** (onglets Actifs/Vulnérabilités/Mitigations auto, criticité éditable, « Importer la sélection » → inventaire réel, bannière « rien n'est écrit tant que vous n'importez pas »). Écritures gardées par `scanner:*`. `tsc -b` + `vite build` verts. Détail dans `ROADMAP.md` Module 6.
20. ~~Auth — durcissement complet de l'architecture 7 couches~~ ✅ fait + **vérifié live de bout en bout le 15/07/2026** (branche `feat/auth-hardening-7layers`, commit `118b1620`). L'audit du 15/07 avait trouvé L2/L3/L4/L5/L7 cassées ; toutes corrigées et prouvées live (Postgres:5434 + Redis, admin@opendefender.io). **JWT — source unique** : `pkg/auth` est désormais la SEULE implémentation (Claims/génération/validation/clés) ; `internal/auth` n'en est plus qu'un alias mince ; `GenerateTypedToken` frappe access ET MFA_REQUIRED ; un seul jeu de clés RSA partagé middleware + `TokenManager`. **L2 OAuth2/SAML** : les deux callbacks émettent maintenant une paire RS256 via `TokenManager.IssueSession` (étaient HS256, rejetées par le middleware RS256) ; nouveau `handler.issueSSOSession` (internal/handler/sso_session.go) intègre l'utilisateur SSO dans un tenant (org perso + membership si absent) → tenant_id réel → routes protégées OK. **L3 Refresh** : `RefreshTokenPair` **supprime** le token présenté avant d'en émettre un nouveau (rotation à usage unique ; rejeu → 401) ; un `SessionResolver` (câblé dans main.go depuis userRepo) re-dérive tenant/rôles/permissions à chaque refresh (claims préservés ET rafraîchis) ; empreinte device comparée correctement (stockée vs présentée) + repli sur l'en-tête `X-Device-Fingerprint`. Live : rotate 200 / rejeu 401 / mauvaise empreinte 401. **L4 MFA** : le login renvoie `{mfa_required, mfa_token}` (aucune session) si secret MFA vérifié ; paire complète seulement après `POST /auth/mfa/challenge` (MFATokenMiddleware) validant TOTP ou code de secours à usage unique ; routes setup/verify/disable (session pleine) + challenge ; clé `MFA_ENCRYPTION_KEY`. Live : setup→verify→login bloqué→challenge(TOTP/secours)→paire ; code faux 400 ; réutilisation code secours 400. **L5 PAT** : format de jeton corrigé (`CreateToken` renvoie `"<prefix8hex>_<secret>"` accepté par `ValidateToken` — tous les PAT échouaient avant) ; `PATMiddleware` monté AVANT la porte JWT (no-op pour les JWT ; AuthMiddlewareRS256 saute si `is_pat`), pose le contexte tenant complet avec permissions = intersection(perms du propriétaire, scopes du PAT) ; CRUD à `/auth/pat`. Live : in-scope 200 / hors-scope 403 / `*` 200 / coexistence JWT 200. **L7 Audit** : login/refresh/logout/oauth/saml/mfa/pat écrivent dans `auth_audit_logs` via `internal/auth.AuditService` (enfin câblé) — IP, UA, géo (en-têtes CDN), empreinte device, timestamp, succès + raison d'échec ; **bug GORM `default:true` corrigé** (les échecs étaient loggés en succès). **AutoMigrate** : ajout de `mfa_secrets`/`mfa_backup_codes`/`personal_access_tokens`/`user_oauth_providers`/`auth_audit_logs` (absentes → 500). Tests : `TestSetupMFA_Success` réparé (clé 31→32 o) + tests unitaires pkg/auth (round-trip JWT, token typé, mauvaise clé, expiration) et PAT (scope/forme). **Restes honnêtes** : les jambes IdP réelles OAuth/SAML non exercées live (pas de credentials — endpoints live, minteur de jeton prouvé via le même `IssueSession` que le challenge MFA) ; `TestRiskCRUDFlow` toujours rouge (pré-existant, hors auth).
21. ~~CTI / Intel Threat — moteur live + CVE→actifs→risques auto + scanner détecte l'application d'une mitigation~~ ✅ fait + **vérifié live le 16/07/2026** (branche `feat/cti-intel-threat-engine`, commit `803ef21d`, poussée ; app tournait en arrière-plan pour l'utilisateur). Le moteur CTI (`pkg/cti` : client NVD/CISA, sync worker, matcher, repo) existait mais était **totalement non câblé**. **(a) Feeds :** `cti_vulnerabilities` ajouté à `AutoMigrate` ; **MITRE ATT&CK embarqué** (`//go:embed pkg/cti/data/mitre_attack.json`, 18 mappings CVE→technique/tactique) ; handler CTI + routes (`GET /cti/vulnerabilities` recherche, `/cti/vulnerabilities/:cve`, `/cti/stats`, `POST /cti/sync` admin, `POST /cti/match` admin) ; SyncWorker câblé (NVD 1h, CISA KEV 6h, tout CISA → `cisa_known`+criticité max, matching post-sync), périodique derrière `CTI_SYNC_ENABLED` (manuel toujours live), clé `NVD_API_KEY`. **Page Intel Threat refaite sur données réelles** (`ThreatIntel.tsx` : stats + flux CVE live avec badges CISA-KEV + techniques MITRE, filtre recherche/sévérité, boutons Sync/Match, 3 états ; `ctiService`+`useCTI`). Preuve : **1644 CVE synchronisées** (NVD+CISA), Log4Shell enrichi TA0001/TA0002 + T1190/T1059. **(b) CVE→actifs→risques :** champ `CPEs text[]` ajouté à `domain.Asset` ; `TenantSweepMatcher` (impl `cti.Matcher`) + `AutoRiskCreator` (impl `cti.RiskCreator`) balaient les actifs par tenant, intersectent les CPE avec les CVE, **auto-créent un risque** (`Source=cti_auto`, `SourceCVEID`, `AssetID`), idempotent ; CISA-known → probabilité 0.9. Preuve : 1 risque auto-créé depuis CVE-2021-44228 sur l'actif log4j ; re-run = 0 (idempotent). **(c) Scanner détecte l'application d'une mitigation :** seam `MitigationAutoDetector` sur le `Pipeline` (`WithMitigationDetector`) — après un scan, un finding (CVE) qui n'est plus détecté remonte finding→actif→risque(`source_cve_id`)→plan de mitigation→sous-action et l'**auto-complète** (`CompletedSource=scanner`, `CompletedBy=nil`, `AutoDetectedAt=now`), puis publie `mitigation.auto_completed {tenant_id, plan_id, sub_action_id, scanner_job_id}`. **SSE** `GET /mitigations/events?token=` (monté avant la porte JWT car EventSource ne peut pas envoyer de header Bearer), filtré par tenant ; front `useMitigationEvents` rafraîchit le board → le badge « Auto-détecté » (déjà présent dans `SubActionTable`) apparaît en live ; complétion/annulation manuelle toujours dispo. **Restes honnêtes :** le SSE suit le même pattern fasthttp que le SSE scanner (prouvé par parité + publish live) mais la livraison de bout en bout n'a pas été exercée via un double-scan complet (montage lourd) ; l'énumération NVD réelle a renvoyé 0 sur la fenêtre 2h sans clé (les données live viennent de CISA KEV, sans CPE — le matching a été prouvé sur une CVE NVD avec CPE injectée). `go build`/`vet`/tests verts (sauf `TestRiskCRUDFlow` pré-existant), `tsc -b`/`vite build` verts.
13. Hors scope immédiat mais à planifier : passe d'animation complète sur les pages restantes (Reports, Marketplace, CustomFields, Users, Roles, Tenants, AuditLogs, TokenManagement — non touchées cette session, seul le Dashboard a reçu un vrai polish) ; les deux vocabulaires `RiskStatus` (lowercase vs uppercase) ne sont pas unifiés, seulement rendus non-fatals côté frontend — un vrai nettoyage nécessiterait de choisir un seul vocabulaire côté backend ; `CreateRisk` ne renseigne pas `created_by` depuis le contexte réel ; implémenter `/analytics/security-score` et `/analytics/assets/statistics` (routes jamais créées, widgets en repli gracieux) ; ~350 findings lint frontend ; 7 fichiers de tests frontend en échec (pré-existants, build) ; rétrofit du client OpenAPI généré sur Risk/Mitigation (pré-existant, M1) ; `TestRiskCRUDFlow`/`TestSetupMFA_Success` en échec (pré-existants, découverts pendant M1 — `TestStartAndStop`/`TestRateLimit_DifferentIPs` flaky, repassent au vert en isolation) ; `src/hooks/useAssetStore.ts` reste un chemin de données non contract-first (utilisé par les sélecteurs d'assets de `CreateRiskModal`/`EditRiskModal`/`DashboardGrid`, volontairement non touché pendant M3).

