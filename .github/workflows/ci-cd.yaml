name: OpenRisk CI/CD

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}
      - name: Build and push dashboard-frontend
        run: |
          cd dashboard/frontend
          docker build -t ${{ secrets.DOCKER_HUB_USERNAME }}/openrisk-frontend:latest .
          docker push ${{ secrets.DOCKER_HUB_USERNAME }}/openrisk-frontend:latest

  build-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}
      - name: Build and push dashboard-backend
        run: |
          cd dashboard/backend
          docker build -t ${{ secrets.DOCKER_HUB_USERNAME }}/openrisk-backend:latest .
          docker push ${{ secrets.DOCKER_HUB_USERNAME }}/openrisk-backend:latest

  build-sync-engine:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}
      - name: Build and push sync-engine
        run: |
          cd sync-engine
          docker build -t ${{ secrets.DOCKER_HUB_USERNAME }}/openrisk-sync-engine:latest .
          docker push ${{ secrets.DOCKER_HUB_USERNAME }}/openrisk-sync-engine:latest

  # test:
  #   runs-on: ubuntu-latest
  #   needs: [build-frontend, build-backend, build-sync-engine]  # Parallel builds before test
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Set up Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.11'
  #     - name: Install dependencies
  #       run: |
  #         cd sync-engine
  #         pip install -r requirements.txt
  #     - name: Run unit tests
  #       run: |
  #         cd sync-engine
  #         pytest tests.py || echo "Tests failed - Check code"  # Graceful if file missing, but now exists

# deploy:
#   runs-on: ubuntu-latest
#   needs: test
#   if: github.ref == 'refs/heads/main'
#   steps:
#     - uses: actions/checkout@v4
#     - name: Lint Helm Chart
#       run: helm lint infra/helm
#     - name: Set up Helm
#       uses: azure/setup-helm@v4
#     - name: Set up kubectl
#       uses: azure/setup-kubectl@v4
#     - name: Configure kubeconfig
#       run: |
#         mkdir -p ~/.kube
#         echo "${{ secrets.KUBE_CONFIG }}" | base64 -d > ~/.kube/config
#     - name: Deploy to K8s
#       run: |
#         helm upgrade --install openrisk infra/helm --namespace openrisk --create-namespace \
#           --set backend.image=${{ secrets.DOCKER_HUB_USERNAME }}/openrisk-backend:latest \
#           --set frontend.image=${{ secrets.DOCKER_HUB_USERNAME }}/openrisk-frontend:latest \
#           --set dbDsn=${{ secrets.DB_DSN }} \
#           --set jwtSecret=${{ secrets.JWT_SECRET }} \
#           --set postgresPassword=${{ secrets.POSTGRES_PASSWORD }}