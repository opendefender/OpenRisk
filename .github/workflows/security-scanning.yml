name: Security Scanning

on:
  push:
    branches: [master, develop]
  pull_request:
    branches: [master, develop]
  schedule:
    - cron: '0 2 * * *'  # Daily at 2 AM

jobs:
  # Go modules, npm production dependencies and the published image are gated
  # by dependency-gate.yml (#487). The govulncheck step, the `npm audit || true`
  # step and the scan of backend/Dockerfile that were here are replaced by it.

  sast:
    runs-on: ubuntu-latest
    name: SAST Analysis
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v4
        with:
          go-version: '1.25.4'
      
      - name: Gosec - Go Security
        uses: securego/gosec@master
        with:
          args: '-no-fail -fmt sarif -out gosec.sarif ./backend/...'
      
      - name: Upload Gosec report
        uses: github/codeql-action/upload-sarif@v2
        with:
          sarif_file: gosec.sarif
          category: gosec

  secret-scan:
    runs-on: ubuntu-latest
    name: Secret Detection
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      
      - name: TruffleHog scan
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ${{ github.event.before || github.event.repository.default_branch }}
          head: ${{ github.event.after || github.sha }}
          extra_args: --debug --only-verified

  sca:
    runs-on: ubuntu-latest
    name: Software Composition Analysis
    steps:
      - uses: actions/checkout@v3

      - name: OWASP Dependency-Check
        uses: dependency-check/Dependency-Check_Action@main
        env:
          # Optional: set the repo secret NVD_API_KEY to enable/speed up NVD downloads.
          NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
        with:
          project: 'OpenRisk'
          path: '.'
          format: 'HTML'
          # Disable the online analyzers that now require credentials / an external
          # API and were failing this job (Node Audit hit the NPM Audit API; OSS Index
          # needs a Sonatype token). Go module and npm vulns are gated by
          # dependency-gate.yml.
          args: >-
            --disableNodeAudit
            --disableYarnAudit
            --disableOssIndex

      - name: Upload dependency-check report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dependency-check-report
          path: reports/
          if-no-files-found: ignore

  code-quality:
    runs-on: ubuntu-latest
    name: Code Quality & Security
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      
      - name: SonarQube Analysis
        uses: sonarsource/sonarqube-scan-action@master
        env:
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        continue-on-error: true

  report:
    if: always()
    runs-on: ubuntu-latest
    needs: [sast, secret-scan, sca, code-quality]
    name: Security Report
    steps:
      - name: Create security summary
        run: |
          echo "# Security Scan Results" >> $GITHUB_STEP_SUMMARY
          echo "- SAST Analysis: Completed" >> $GITHUB_STEP_SUMMARY
          echo "- Secret Detection: Completed" >> $GITHUB_STEP_SUMMARY
          echo "- SCA Analysis: Completed" >> $GITHUB_STEP_SUMMARY
          echo "- Code Quality: Completed" >> $GITHUB_STEP_SUMMARY
