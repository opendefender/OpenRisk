// The OpenRisk Scanner Agent is a standalone, dependency-free (stdlib only)
// module so the binary stays tiny (< 15 MB) and shares nothing with the SaaS
// backend's dependency tree.
module github.com/opendefender/openrisk-agent

go 1.25
